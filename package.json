{
  "name": "typescript-resolve-plugin",
  "version": "1.0.0",
  "license": "MIT",
  "repository": {
    "url": "https://github.com/generalov/typescript-resolve-plugin"
  },
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib/**/*.js",
    "lib/**/*.ts"
  ],
  "devDependencies": {
    "eslint": "^5.16.0",
    "prettier": "^1.18.2",
    "typescript": "^3.5.1"
  },
  "dependencies": {
    "@types/node": "^12.0.7"
  },
  "scripts": {
    "build": "tsc -p .",
    "lint": "prettier --write **/*.{ts,md,yml,json}",
    "postversion": "git push && git push --tags",
    "prepublishOnly": "yarn lint && yarn build"
  }
}
